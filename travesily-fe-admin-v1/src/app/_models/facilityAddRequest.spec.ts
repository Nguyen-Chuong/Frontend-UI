import { FacilityAddRequest } from "./facilityAddRequest";

describe('FacilityAddRequest', () => {
  it('should create an instance', () => {
    expect(new FacilityAddRequest()).toBeTruthy();
  });
});
